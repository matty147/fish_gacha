manage = instance_find(manager,0);
gacha = instance_find(obj_gacha,0);

show_upgrades = false;
numb_of_upgrades = 5;

upgrade_cost =[0,0




];

if button_type
{
	
	//spawn obj_upgrade_bar into a array so i can fetch the data from it.
	
	
	
}